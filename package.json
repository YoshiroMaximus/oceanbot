{
  "name": "oceanbot",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "register": "node --env-file=.env register.js",
    "profile": "node --env-file=.env set-profile.js",
    "test": "node test/run-tests.mjs"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
